{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .spago",
    "build": "spago build --censor-lib --strict",
    "test": "spago test"
  },
  "devDependencies": {
    "rimraf": "^2.5.4"
  }
}
